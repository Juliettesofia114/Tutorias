<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.tut">

    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="TutoriasUN"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.Cuenta.ver_cuenta"
            android:label="Cuenta"
            ></activity>
        <activity
            android:name=".ui.login.Registro"
            android:label="Registro"
            />
        <activity
            android:name=".ui.Cuenta.Cuenta"
            android:label="Cuenta"
            />
        <activity
            android:name=".ui.mensajeria.mensajeria"
            android:label="Mensajes"
            />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            />

        <service android:name=".MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".datos"
            android:label="Cuenta"
            />
        <activity
            android:name=".ui.Cuenta.password"
            android:label="Restablecer contraseña"
            />
        <activity
            android:name=".Crear"
            android:label="Nueva tutoría"
            />
        <activity
            android:name=".Recordatorios"
            android:label="Tutorías"/>
        <activity
            android:name=".ui.login.Login"
            android:label= "Inicio de Sesión"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>